feat(relation): 整合独立交往率模型并修复前端快照误判

1. 重构底层评估理论 (`manualRelations.js`)：
   - 弃用单一的“缺席率”判定，升级为“综合独立交往率”（结合缺席在场+快照在场）。
   - 在遍历检索时实时校验时间戳特征捕获“未知”快照（在席但不受玩家干预的同步加入）。
   - 彻底解决了无 GPS 视野目标群体（红黄灯、非好友）因无法累计缺席得分而遭惩罚的漏洞。
   - 完善了渲染视图的 Tooltip 详情，详细展现各项在场与缺席等衍生参数。
2. 修复可视化视图渲染：
   - 修复 `TwoPersonRelationship.vue` 中对自我留存时长的非对象化存取造成的 `undefined` 解析错误，该错误先前导致页面丧失了对所有本地快照的数学拦截并无脑呈现为“双向奔赴”。
   - 更新“未知”标签为更低侵入性的灰底样式。
