feat(relation): 深度重构关系推荐算法与观测逻辑

1. 重构算法计分模型：
   - 引入实例层级权重分级（Friends: 1.8x, Friends+: 1.2x, Public: 0.5x）。
   - 新增“双向开房”探测，赋予互为访客的配对 1.2x 连带权重。
   - 移除 Invite+ 的“一票肯定”逻辑，回归基础权重计算。
2. 优化观测效能：
   - 引入动态活跃窗口。改用实际相遇跨度计算密度分，大幅提升短期高频接触者的排名。
   - 针对非好友引入“降维豁免”。彻底移除其缺席率惩罚，并补偿 1.5x 侦查盲区红利。
3. 增强数据透明度与 UI：
   - 推荐列表现在包含并展示已记录好友的计算分值（显示为“已记录”）。
   - 重构 Tooltip。详尽展示基数构成、天数跨度及权重公式。
4. 修复“双向奔赴”误报：
   - 在双人关系图中引入“本地快照”拦截逻辑。
   - 针对黄灯/隐私状态下的同步加入行为，利用数学时间重叠特征将其标记为“未知”，而非误导性的“双向奔赴”。
